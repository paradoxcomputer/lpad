[package]
name = "bonding_curve-methods"
version = "0.1.0"
edition = "2024"

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", default-features = false, features = ["client", "std"] }
bonding_curve_core = { path = "../core" }

[package.metadata.risc0]
methods = ["guest"]
